---
# Ignore misconfigurations
misconfigurations:
  # Offending values are hardcoded within core module variable `connectivty`. Until networking is removed, these must be ignored.
  - id: AVD-AZU-0050 # Security group rule allows unrestricted ingress to SSH port from any IP address.
  - id: AVD-AZU-0047 # Security group rule allows unrestricted ingress from any IP address.
  - id: AVD-AZU-0012
    path: infra/modules/new_resources_agent_capability_host_connections/main.tf
    reason: "Used for simplified public access environment"
  - id: DS-0001
    path: strategy-builder/components/azcred/typescript/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/csharp-microsoft-agent-framework/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/typescript-foundry-agent-service/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/typescript-openai-agent-sdk/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
