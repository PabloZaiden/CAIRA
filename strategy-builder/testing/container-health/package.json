{
  "name": "caira-container-health",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Container build and health check validator — builds Docker images, starts containers, and validates health endpoints",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "validate": "node src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "execa": "^9",
    "vitest": "^4.1.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
