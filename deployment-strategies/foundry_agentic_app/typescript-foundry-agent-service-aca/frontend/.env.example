# API base URL (default: /api, proxied by Vite dev server or nginx)
VITE_API_BASE_URL=/api

# Use SSE streaming for chat messages (default: true, set to "false" to use JSON)
# VITE_USE_STREAMING=true
