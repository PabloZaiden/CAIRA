# CAIRA docs

CAIRA is organized as independent reference components for coding agents. Start with the root `README.md` for the component map and `docs/developer.md` for contribution guidance.

Each component should stand on its own, validate on its own, and be useful as reference material for the CAIRA skill.
