# CAIRA docs

CAIRA beta2 is organized as independent reference components for coding agents. Start with the root `README.md` for the component map and `docs/developer.md` for contribution guidance.

The key rule for new work is simplicity: each component should stand on its own, validate on its own, and be useful as reference material for the CAIRA skill.
