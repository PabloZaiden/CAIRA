---
# Ignore misconfigurations
misconfigurations:
  # Offending values are hardcoded within core module variable `connectivity`. Until networking is removed, these must be ignored.
  - id: AVD-AZU-0050 # Security group rule allows unrestricted ingress to SSH port from any IP address.
  - id: AVD-AZU-0047 # Security group rule allows unrestricted ingress from any IP address.
  - id: DS-0001
    path: strategy-builder/components/azcred/typescript/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/foundry_agentic_app/csharp-microsoft-agent-framework-aca/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/foundry_agentic_app/typescript-foundry-agent-service-aca/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
  - id: DS-0001
    path: deployment-strategies/foundry_agentic_app/typescript-openai-agent-sdk-aca/azcred/Dockerfile
    reason: "Local-only credential sidecar intentionally tracks Azure CLI upstream tags for auth compatibility."
